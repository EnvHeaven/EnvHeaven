module.exports = {
  inspect() {
    return {
      diagnostics: [
        {
          severity: "info",
          code: "plugin-inspected",
          message: "Test plugin inspected the repo."
        }
      ],
      details: {
        plugin: "envheaven-test-plugin"
      }
    };
  },
  async execute(plan) {
    return {
      exitCode: 0,
      details: {
        executedTarget: plan.target
      }
    };
  }
};
