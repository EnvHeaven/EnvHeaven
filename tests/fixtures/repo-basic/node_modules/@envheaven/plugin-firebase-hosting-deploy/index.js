module.exports = {
  inspect() {
    return {
      details: {
        plugin: "@envheaven/plugin-firebase-hosting-deploy"
      }
    };
  },
  async execute(plan) {
    return {
      exitCode: 0,
      details: {
        executedTarget: plan.requestedTarget,
        resolvedTarget: plan.resolvedTarget
      }
    };
  }
};
